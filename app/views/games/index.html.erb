<h1>List of Games</h1>

<ul>
  <% @games.available.each do |game| %>
    <li>
      <h4><%= game.name %></h4>
      <%= button_to 'Join Game', game_path(game), :method=> :put %>
    </li>
  <% end %>
</ul>
